{
  "name": "modifiedtodolist",
  "version": "1.0.0",
  "description": "This is basic web app where a user can store all the tasks that are required to be done, with the deadline.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "to-do-list"
  ],
  "author": "milind d jain",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "ejs": "^3.1.8",
    "express": "^4.18.1",
    "mongoose": "^6.5.2"
  }
}
